int main()
{
        float a1 = 1; //переменная начала отсчета
        float a2 = 5; //переменная конца отсчета
        float n = (a2 - a1 + 1) / 2; //формула расчета кол-ва чисел в диапазоне
        float sum = (a1 + a2) * n; //формула расчета суммы чисел в диапазоне
        return 0;
}
